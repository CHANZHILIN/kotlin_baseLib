<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Toolbar的Title大小-->
    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">18sp</item>
    </style>


    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
        <!-- 窗口背景色 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否有覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!--进出动画-->
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>

    <!--圆形图像-->
    <declare-styleable name="CircleImageView">
        <attr name="border_width" format="dimension" />
        <attr name="border_color" format="color" />
    </declare-styleable>

    <!-- Actvitiy 透明样式 - 无标题, 全屏, 背景透明等 -->
    <style name="DevActivityTranslucentTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:activityOpenEnterAnimation">@null</item>
        <item name="android:activityOpenExitAnimation">@null</item>
        <item name="android:activityCloseEnterAnimation">@null</item>
        <item name="android:activityCloseExitAnimation">@null</item>
        </style>
    <!--Toolbar的Theme-->
    <style name="toolbar_action_menu_overflow" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--是否覆盖锚点-->
        <item name="overlapAnchor">false</item>
        <!--弹出层水平方向上的偏移，负值为距离右边的空隙-->
        <item name="android:dropDownHorizontalOffset">-10dp</item>
        <!-- 弹出层垂直方向上的偏移，负则会盖住Toolbar -->
        <item name="android:dropDownVerticalOffset">5dp</item>
    </style>


    <!--圆形按钮-->
    <declare-styleable name="CircleButton">
        <attr name="circle_background" format="color|reference" />
        <attr name="circle_text" format="string|reference" />
        <attr name="circle_textSize" format="dimension" />
        <attr name="circle_textColor" format="color|reference" />
    </declare-styleable>

</resources>
